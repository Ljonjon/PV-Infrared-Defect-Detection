# Runtime dependencies for PV infrared inference
# PyTorch must be installed with a CUDA-compatible build when GPU inference is required.
torch>=2.0
torchvision>=0.15
numpy>=1.24
opencv-python>=4.8
Pillow>=9.5
PyYAML>=6.0
scipy>=1.10
tqdm>=4.65
matplotlib>=3.7
pandas>=2.0
seaborn>=0.12
requests>=2.31
setuptools>=68

# Optional: model export and FLOPs profiling
# onnx>=1.14
# coremltools>=7.0
# thop>=0.1.1
